## 1. 安装依赖

- [x] 1.1 在 `apps/client` 安装 `@uiw/react-md-editor`

## 2. 创建 MarkdownEditor 组件

- [x] 2.1 在 `apps/client/src/components/` 下创建 `MarkdownEditor.tsx` 通用组件
- [x] 2.2 支持编辑/预览双模式切换
- [x] 2.3 适配暗色主题

## 3. 集成到 ShotProperties

- [x] 3.1 替换 `Input.TextArea` 为 `MarkdownEditor`，保持原有保存逻辑
- [x] 3.2 新增"放大编辑"按钮及 Modal 实现
- [x] 3.3 行内编辑器和 Modal 编辑器状态同步
