## 1. Chatbox Layout & Interaction

- [x] 1.1 将 `ChatboxPanel` 默认宽度从当前值调整为更宽（目标 420）并抽离为可配置常量
- [x] 1.2 在窄屏场景实现宽度回退或折叠策略，避免挤压流程画布
- [x] 1.3 优化 chatbox 区块布局（消息区/摘要区/约束区）以支持更长文本展示
- [x] 1.4 调整消息状态管理为“追加式展示”，禁止阶段事件覆盖历史对话正文

## 2. Character Consistency Model

- [x] 2.1 设计并实现角色档案数据结构（外观、服饰、关键特征、禁改项）
- [x] 2.2 在每轮分镜生成前从历史输入与当前草案提取/更新角色档案
- [x] 2.3 将角色档案持久化关联到草案版本或项目会话上下文
- [x] 2.4 在后续生成请求中注入角色档案硬约束，保证跨镜头一致性

## 3. Director-Guided Prompting

- [x] 3.1 重构分镜生成 system prompt 为“视频脚本专家 + 分镜导演”双角色模板
- [x] 3.2 增加关键澄清机制（角色、风格、节奏、叙事目标缺失时触发）
- [x] 3.3 支持快速模式/精细模式两种交互深度（默认快速模式）

## 4. SSE Protocol Enrichment

- [x] 4.1 扩展 SSE 事件协议，新增 `clarification` 与 `constraint-summary` 事件
- [x] 4.2 在后端流式阶段按时机发送新增事件并保持 `progress/token/done/error` 兼容
- [x] 4.3 在前端 SSE parser 中消费新增事件并展示为独立交互卡片或消息块
- [x] 4.4 生成结束后收敛状态消息为摘要，且保留完整对话和 token 过程可回看

## 5. Draft Experience & UX Controls

- [x] 5.1 在 chatbox 展示“当前角色约束摘要”，并允许用户在对话中修正
- [x] 5.2 在分镜生成过程中展示更丰富中间反馈（阶段、约束确认、草案摘要）
- [x] 5.3 在用户对结果不满意时提供受控迭代路径，明确继承上一版本上下文

## 6. Verification

- [x] 6.1 后端测试：角色档案抽取、冲突提示、约束注入、SSE 新事件
- [x] 6.2 前端测试：宽版 chatbox 布局、新增事件渲染、约束摘要展示与更新
- [x] 6.3 端到端验证：多轮对话下角色一致性与用户意图对齐效果
